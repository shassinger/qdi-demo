{
  "name": "QDI Sandbox",
  "image": "mcr.microsoft.com/devcontainers/python:3.10-bullseye",
  
  // Install compiler packages and lightweight Python web dependencies (including simulation packages)
  "postCreateCommand": "sudo apt-get update && sudo DEBIAN_FRONTEND=noninteractive apt-get install -y clang cmake && pip install fastapi uvicorn httpx qiskit qiskit-aer pyqir",

  // Launch the backend API server and static web server using start.sh (non-blocking)
  "postStartCommand": "bash .devcontainer/start.sh",

  // Configure port forwarding
  "forwardPorts": [8000, 3000],
  "portsAttributes": {
    "8000": {
      "label": "QDI Backend API",
      "onAutoForward": "silent",
      "visibility": "public"
    },
    "3000": {
      "label": "QDI Web Console",
      "onAutoForward": "openBrowser",
      "visibility": "public"
    }
  },

  // Open index.html in the editor panel automatically
  "customizations": {
    "codespaces": {
      "openFiles": ["qdi-core/python/index.html"]
    }
  }
}
